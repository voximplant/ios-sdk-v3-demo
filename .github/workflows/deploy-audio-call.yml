name: Deploy Audio Call

on:
  workflow_dispatch:
      push:
    branches:
      - github_actions_adopt

jobs:
  deploy:
    uses: ./.github/workflows/deploy-testflight.yml
    with:
      project_dir: AudioCall
      xcodeproj: audio-call.xcodeproj
      scheme: audio-call
      bundle_id: com.voximplant.demos.v3.audiocall
    secrets: inherit
